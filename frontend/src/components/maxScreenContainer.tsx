import React from "react";

type Props = {
    children: React.ReactNode;
};

function maxScreenContainer({ children }: Props) {
    return <div className="">children</div>;
}

export default maxScreenContainer;
